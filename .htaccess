<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is not already pointing to the public folder
    RewriteCond %{REQUEST_URI} !^/public/

    # Rewrite all requests to the public/ directory securely
    RewriteRule ^(.*)$ public/$1 [L,QSA]
</IfModule>
